# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.env

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/


.idea
setup_test.sh
Makefile
deploy
test
tmp
.terraform/
terraform.tfstate*
.terraform.lock.hcl
.terraform/
terraform.tfstate*
.terraform.lock.hcl

# Terraform standard ignores
.terraform/
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
